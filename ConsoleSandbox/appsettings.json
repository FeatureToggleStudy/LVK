{
    "LoggingFilename": "${env.DROPBOX}\\${sys.MachineName}-${sys.UserName}.txt",
    "Logging": {
        "Destinations": {
            "Console": {
                "LogLevel": "Information"
            }
        }
    }
}